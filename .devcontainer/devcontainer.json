{
    "name": "C++ Development",
    "image": "ubuntu",
    "containerName": "C++_Development",
    "settings": { 
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "runArgs": [
        "--cap-add=SYS_PTRACE",
        "--security-opt",
        "seccomp=unconfined"
    ],
    "postCreateCommand": "apt-get update && apt-get install -y git ssh gcc g++ make cmake libboost-all-dev ninja-build"
}